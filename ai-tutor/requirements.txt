# AI tutor: Python dependencies (separate from v1's venv).
# Setup, from the repo root:
#   python -m venv ai-tutor/.venv
#   ai-tutor/.venv/Scripts/python -m pip install -r ai-tutor/requirements.txt
# fastembed runs the embedding model with ONNX, so no PyTorch is needed.
fastembed==0.8.1
numpy==2.4.6
# Browser tests of the in-browser Python runner (Milestone 1b). After installing:
#   ai-tutor/.venv/Scripts/python -m playwright install chromium
playwright==1.63.0
