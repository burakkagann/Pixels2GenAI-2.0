---
import { inlineMarkdownHtml } from '@/lib/inlineMarkdown';
interface Props {
  /** Visible label, e.g. "Solution" or "Hint". */
  summary: string;
}

const { summary } = Astro.props;
---

<details class="dropdown">
  <summary set:html={inlineMarkdownHtml(summary)} />
  <div class="dropdown-body">
    <slot />
  </div>
</details>
